import { Component } from "@angular/core";

@Component({
    selector: 'hello',
    template: `
        <h1>Hello World!</h1>
    `
})
export class HelloComponent {

}

// UI (View) - Template or TemplateUrl (Required)
// Style (CSS) - Styles or StyleUrls
// State (Data)
// Behavior (Methods)